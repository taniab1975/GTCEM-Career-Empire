{
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "check": "node scripts/dev-check.mjs",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "ci": "npm run check && npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vitest": "^3.2.0"
  }
}
